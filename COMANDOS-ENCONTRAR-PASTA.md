# ✅ ENCONTROU A PASTA!

## Execute estes comandos:

### 1. Ver o que tem na pasta plenipay:
```bash
ls -la /var/www/plenipay
```

### 2. Ver o que tem em /var/www/html:
```bash
ls -la /var/www/html
```

### 3. Entrar na pasta plenipay:
```bash
cd /var/www/plenipay
ls -la
```

---

## 📋 O QUE FAZER:

**Execute o comando 3 primeiro** (`cd /var/www/plenipay` e depois `ls -la`)

Me diga o que apareceu! 

Se tiver os arquivos do projeto (package.json, server.js, etc.), essa é a pasta certa! 🎯

